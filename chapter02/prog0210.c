#include <stdio.h>

int main (void)
{
        int ch;
        printf("Escreva um caractere: ");
        scanf("%d", &ch);

        printf("O caractere escrito na tela foi : %c\n", ch);
        return (0);
}
