#include <stdio.h>

int main (void)
{
        char Nome[50];
        printf("Introduza o nome completo: ");
        scanf("%s", Nome);
        printf("Nome completo: %s\n", Nome);
        return (0);
}
