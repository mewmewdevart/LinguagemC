#include <stdio.h>

int main (void)
{
        int number;

        number = 0;
        while (number <= 10)
        {
                printf("%d\n", number);
                number++;
        }
        return (0);
}
