#include <stdio.h>

int main (void)
{
        int number;

        number = 10;
        while (number)
        {
                printf("%d\n", number);
                number--;
        }
        return (0);
}
