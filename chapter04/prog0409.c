#include <stdio.h>

int main (void)
{
        int number;

        number = 2;
        while (number <= 20)
        {
                printf("%d\n", number);
                number = number + 2;
        }
        return (0);
}
